

<view class='control'>
  <block wx:for="{{list}}" wx:key="{{index}}">
   <view class='control-item {{index == currentIndex ? "isActive": "" }}' bind:tap="handlebindtap" data-index="{{index}}"><text>{{item}}</text></view>
    
  </block>
</view>